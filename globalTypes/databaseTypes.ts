export enum ExpertiseLevel {
  beginner = "beginner",
  intermediate = "intermediate",
  expert = "expert",
}

export type Area = "sales" | "engineering" | "marketing" | "design";
export enum Visibility {
  private = "private",
  public = "public",
}

export enum RemoteLevel {
  none = "none",
  hybrid = "hybrid",
  full = "full",
}

export enum JobStatus {
  cancelled = "cancelled",
  posted = "posted",
  current = "current",
  completed = "completed",
}

export enum JobLength {
  short = "short",
  medium = "medium",
  long = "long",
}

export enum ProposalStatus {
  cancelled = "cancelled",
  rejected = "rejected",
  sent = "sent",
  accepted = "accepted",
}
